#include <stdio.h>

int main()
{
	#include <stdio.h>
	int lineas, caracteres, c;
	lineas = 0;
	caracteres = 0;
 	while ((c=getchar()) != EOF){
		if (c == '\n')
			lineas ++;
		else 
			caracteres ++;
	}
	printf("Lineas %d \n", lineas);
	printf("Caracteres %d \n", caracteres); 
	return(0);
}


